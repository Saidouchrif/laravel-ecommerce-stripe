version: "3.9"

services:
  stripe:
    image: stripe/stripe-cli:latest
    container_name: stripe_cli
    environment:
      STRIPE_API_KEY: ${STRIPE_SECRET}
    command: >
      listen
      --forward-to http://host.docker.internal:8000/api/stripe/webhook
